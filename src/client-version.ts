/** Keep aligned with package.json; the release test rejects a mismatch. */
export const CLIENT_VERSION = "0.18.5";
export const CLIENT_USER_AGENT = `AssignmentGuide/${CLIENT_VERSION} (course-client)`;
